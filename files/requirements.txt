streamlit>=1.38,<2.0
python-dotenv>=1.0,<2.0
faster-whisper>=1.1,<2.0
edge-tts>=6.1,<7.0
pydantic>=2.0,<3.0

# LLM SDK — uncomment exactly ONE matching LLM_PROVIDER in .env.
# Default: Gemini (uncommented). Comment this out if using another provider.
google-generativeai>=0.8,<1.0   # for Gemini (DEFAULT)
# openai>=1.0,<2.0              # for OpenAI
# anthropic>=0.30,<1.0          # for Anthropic
